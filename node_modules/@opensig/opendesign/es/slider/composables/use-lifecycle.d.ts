import { sliderPropsT, SliderInitData } from '../types';
export declare const useLifecycle: (props: sliderPropsT, initData: SliderInitData, resetSize: () => void) => {
    slider: import('vue').Ref<HTMLElement | undefined, HTMLElement | undefined>;
};
