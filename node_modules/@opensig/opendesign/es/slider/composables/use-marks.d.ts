import { sliderPropsT, sliderMarksPropsT } from '../types';
export interface Mark extends sliderMarksPropsT {
    point: number;
    position: number;
}
export declare const useMarks: (props: sliderPropsT) => import('vue').ComputedRef<Mark[]>;
