import { CSSProperties, ComputedRef } from 'vue';
import { SliderInitData, sliderPropsT } from '../types';
type Stops = {
    stops: ComputedRef<Array<{
        step: number;
        reached: boolean;
    }>>;
    getStopStyle: (position: number) => CSSProperties;
};
export declare const useStops: (props: sliderPropsT, initData: SliderInitData, minValue: ComputedRef<number>, maxValue: ComputedRef<number>) => Stops;
export {};
