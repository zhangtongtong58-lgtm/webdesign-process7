"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const useResizeObserver = require("./use-resize-observer.js");
const is = require("../_utils/is.js");
let ro = null;
function useReiszeObserverDirective(onResize) {
  return {
    vResizeObserver: {
      beforeMount() {
        ro = useResizeObserver.useResizeObserver();
      },
      mounted(el) {
        if (is.isFunction(onResize)) {
          ro == null ? void 0 : ro.observe(el, onResize);
        }
      },
      unmounted(el) {
        if (onResize) {
          ro == null ? void 0 : ro.unobserve(el, onResize);
        }
      }
    }
  };
}
exports.useReiszeObserverDirective = useReiszeObserverDirective;
