declare function addListener(el: HTMLElement, fn: () => void, params?: {
    fast?: boolean;
    exception?: (e: Event) => boolean;
}): void;
declare function removeListener(el: HTMLElement, listener?: () => void): void;
/**
 * 外部点击
 * @param fast 是否在mousedown、touchstart时就触发事件，主要用于在blur事件之前处理回调
 */
export declare function useOutClick(): {
    addListener: typeof addListener;
    removeListener: typeof removeListener;
};
export {};
