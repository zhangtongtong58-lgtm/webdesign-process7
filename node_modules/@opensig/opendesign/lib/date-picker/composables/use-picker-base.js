"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const vue = require("vue");
const useFormField = require("../../_composables/use-form-field.js");
const provide = require("../provide.js");
const useTimestampValue = require("./use-timestamp-value.js");
function usePickerBase(opts) {
  const { props, mode, modelValue, emit } = opts;
  const propsRefs = vue.toRefs(props);
  const formField = useFormField.useFormField(props, emit);
  const timestampValue = useTimestampValue.useTimestampValue(modelValue, propsRefs.valueFormat);
  const modeRef = vue.computed(() => mode);
  vue.provide(provide.datePickerInjectKey, {
    ...propsRefs,
    mode: modeRef,
    color: formField.effectiveColor,
    disabledMonth: propsRefs.disabledMonth,
    disabledYear: propsRefs.disabledYear
  });
  return { timestampValue, ...formField };
}
exports.usePickerBase = usePickerBase;
