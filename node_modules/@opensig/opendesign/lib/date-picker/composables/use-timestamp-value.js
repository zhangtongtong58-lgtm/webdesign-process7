"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const vue = require("vue");
const dayjs = require("dayjs");
const is = require("../../_utils/is.js");
const utils = require("../utils.js");
function useTimestampValue(modelValue, valueFormat) {
  return vue.computed({
    get() {
      var _a;
      return (_a = utils.parseValue(modelValue.value)) == null ? void 0 : _a.valueOf();
    },
    set(ts) {
      if (is.isNil(ts)) {
        modelValue.value = void 0;
        return;
      }
      if (vue.toValue(valueFormat) === "x") {
        modelValue.value = ts;
      } else {
        modelValue.value = dayjs(ts).format(vue.toValue(valueFormat));
      }
    }
  });
}
exports.useTimestampValue = useTimestampValue;
