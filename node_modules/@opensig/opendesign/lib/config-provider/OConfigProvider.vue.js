"use strict";
const vue = require("vue");
const types = require("./types.js");
const provide = require("./provide.js");
const _sfc_main = /* @__PURE__ */ vue.defineComponent({
  __name: "OConfigProvider",
  props: types.configProviderProps,
  setup(__props) {
    const props = __props;
    const { locale, link } = vue.toRefs(props);
    const globalConfig = vue.reactive({
      locale,
      link
    });
    vue.provide(provide.configProviderInjectKey, globalConfig);
    return (_ctx, _cache) => {
      return vue.renderSlot(_ctx.$slots, "default");
    };
  }
});
module.exports = _sfc_main;
