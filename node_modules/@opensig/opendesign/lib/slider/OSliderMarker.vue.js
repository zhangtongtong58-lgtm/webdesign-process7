"use strict";
const vue = require("vue");
const is = require("../_utils/is.js");
const types = require("./types.js");
const _sfc_main = /* @__PURE__ */ vue.defineComponent({
  __name: "OSliderMarker",
  props: types.sliderMarksProps,
  setup(__props) {
    const props = __props;
    const label = vue.computed(() => {
      return is.isString(props.mark) ? props.mark : props.mark.label;
    });
    const style = vue.computed(() => is.isString(props.mark) ? void 0 : props.mark.style);
    return (_ctx, _cache) => {
      return vue.openBlock(), vue.createElementBlock(
        "div",
        {
          class: "o-slider-marks-text",
          style: vue.normalizeStyle(style.value)
        },
        vue.toDisplayString(label.value),
        5
        /* TEXT, STYLE */
      );
    };
  }
});
module.exports = _sfc_main;
