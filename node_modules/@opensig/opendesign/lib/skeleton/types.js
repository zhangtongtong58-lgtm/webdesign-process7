"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const skeletonTextProps = {
  /**
   * @zh-CN 文本行数
   * @en-US Number of lines of text.
   * @default 3
   */
  rows: {
    type: Number,
    default: 3
  }
};
const SkeletonAvatarSizeTypes = ["large", "medium", "small", "mini"];
const skeletonAvatarProps = {
  /**
   * @zh-CN 头像尺寸
   * @en-US Avatar size.
   * @default 'medium'
   */
  size: {
    type: String,
    default: "medium"
  },
  /**
   * @zh-CN 圆角值
   * @en-US Round.
   * @default 'pill'
   */
  round: {
    type: String,
    default: "pill"
  }
};
const skeletonFigureProps = {};
const skeletonProps = {
  /**
   * @zh-CN 是否显示加载中状态（即展示骨架屏）
   * @en-US Whether to display the loading status (i.e., show the skeleton screen).
   * @default true
   */
  loading: {
    type: Boolean,
    default: true
  },
  /**
   * @zh-CN 是否展示动画
   * @en-US Whether to display the animation.
   * @default false
   */
  animation: {
    type: Boolean,
    default: false
  },
  /**
   * @zh-CN 文本行数
   * @en-US Number of lines of textd.
   * @default 3
   */
  rows: {
    type: Number,
    default: 3
  }
};
exports.SkeletonAvatarSizeTypes = SkeletonAvatarSizeTypes;
exports.skeletonAvatarProps = skeletonAvatarProps;
exports.skeletonFigureProps = skeletonFigureProps;
exports.skeletonProps = skeletonProps;
exports.skeletonTextProps = skeletonTextProps;
