"use strict";
const vue = require("vue");
const types = require("./types.js");
const styleClass = require("../_utils/style-class.js");
const _sfc_main = /* @__PURE__ */ vue.defineComponent({
  __name: "OSkeletonAvatar",
  props: types.skeletonAvatarProps,
  setup(__props) {
    const props = __props;
    const round = styleClass.getRoundClass(props, "skeleton");
    return (_ctx, _cache) => {
      return vue.openBlock(), vue.createElementBlock(
        "div",
        {
          class: vue.normalizeClass(["o-skeleton-item o-skeleton-avatar", [`o-skeleton-avatar-${props.size}`, vue.unref(round).class.value]]),
          style: vue.normalizeStyle(vue.unref(round).style.value)
        },
        null,
        6
        /* CLASS, STYLE */
      );
    };
  }
});
module.exports = _sfc_main;
