"use strict";
const vue = require("vue");
const vueUtils = require("../_utils/vue-utils.js");
const OChildOnly = vue.defineComponent({
  name: "OChildOnly",
  setup(_props, { slots }) {
    return () => {
      var _a;
      const children = (_a = slots.default) == null ? void 0 : _a.call(slots);
      return children ? vueUtils.getFirstComponent(children) : null;
    };
  }
});
module.exports = OChildOnly;
