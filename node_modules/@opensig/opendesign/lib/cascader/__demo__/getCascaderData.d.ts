interface TreeNode {
    label: string;
    value: string;
    children?: TreeNode[];
}
/**
 * 生成树形级联数据
 * @param depth 树的最大深度（必须≥1）
 * @param childMinCount 每个节点的最小子节点数
 * @param childMaxCount 每个节点的最大子节点数
 * @returns 生成的树形数据数组
 */
export declare function getCascaderData(depth: number, childMinCount: number, childMaxCount: number): TreeNode[];
export {};
