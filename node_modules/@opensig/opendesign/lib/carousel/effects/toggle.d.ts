import { PointMoveT } from '../../_utils/types';
import { default as Effect, EffectOptionT } from './effect';
export default class Toggle extends Effect {
    private slideList;
    private isChanging;
    private resolveArr;
    constructor(slideElList: HTMLElement[], slideContainer: HTMLElement, activeIndex: number, options?: EffectOptionT);
    handleTouchStart(): void;
    handleTouchMove(): void;
    handleTouchEnd(pos: PointMoveT): number | undefined;
    active(toIndex: number, animate?: boolean, force?: boolean): Promise<null | number>;
    destroyed(): void;
}
