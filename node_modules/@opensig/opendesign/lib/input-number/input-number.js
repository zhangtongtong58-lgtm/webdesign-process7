"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const is = require("../_utils/is.js");
function string2number(value) {
  return value === "" ? NaN : Number(value);
}
function number2string(value) {
  return Number.isNaN(value) || is.isUndefined(value) ? "" : String(value);
}
function isValidNumber(val, min, max, parse) {
  if (Number.isNaN(val)) {
    return false;
  }
  const value = is.isFunction(parse) ? parse(String(val)) : val;
  if (is.isNumber(Number(value))) {
    const v = Number(value);
    if (!is.isUndefined(min) && v < min) {
      return false;
    }
    if (!is.isUndefined(max) && v > max) {
      return false;
    }
    return true;
  }
  return false;
}
function correctValue(val, lastVal, min, max) {
  if (is.isNumber(val)) {
    if (!is.isUndefined(max) && val > max) {
      return max;
    }
    if (!is.isUndefined(min) && val < min) {
      return min;
    }
    return val;
  }
  return lastVal;
}
exports.correctValue = correctValue;
exports.isValidNumber = isValidNumber;
exports.number2string = number2string;
exports.string2number = string2number;
