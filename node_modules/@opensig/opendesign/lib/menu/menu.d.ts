import { VTree, TreeNodeT } from '../_utils/tree';
interface MenuNodeT extends TreeNodeT {
}
export default class MenuTree extends VTree {
    constructor(value: string | number, parent: MenuNodeT | null, children?: Array<MenuNodeT>);
    addChild(options: {
        value: string;
        parentVal: string | undefined;
    }): void;
    selectNode(val: string): (string | undefined)[];
    getSiblings(val: string): (string | number | undefined)[];
}
export {};
