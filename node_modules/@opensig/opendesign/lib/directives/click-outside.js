"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const useOutClick = require("../hooks/use-out-click.js");
let out = null;
const vOutClick = {
  beforeMount(el, binding) {
    out = useOutClick.useOutClick();
    out == null ? void 0 : out.addListener(el, binding.value, {
      fast: binding.modifiers.fast
    });
  },
  unmounted(el) {
    out == null ? void 0 : out.removeListener(el);
  }
};
exports.vOutClick = vOutClick;
